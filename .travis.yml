language: python

python:
  - "2.7"

before_install:
  - sudo apt-get update -qq

install:
  - pip install .

script:
  - python -m doctest checkimpcont.py < "/dev/null"
  - ./tests/test_contorted
  - checkimpcont.py checkimpcont.py
